.env
.env.*
!.env.example

# OS / editor cruft
**/.DS_Store

# NOTE: product hero images and per-product clip.mp4 videos are intentionally
# committed so they deploy to Hostinger. Do not re-add exclusions for them.
